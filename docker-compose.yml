services:
  bookstack-mcp:
    image: ghcr.io/matiassy/bookstack-mcp-server:latest
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      MCP_TRANSPORT: http
      SERVER_PORT: 3000
      LOG_LEVEL: ${LOG_LEVEL:-info}
    restart: unless-stopped
